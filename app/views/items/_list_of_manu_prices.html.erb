<div id="prices_list">

	<!-- <ul>
		<%# @manu_price_history.each do |change| %>
			<%#= number_to_currency change.price %>&nbsp;&nbsp;&nbsp;(<%#= change.start_date.strftime('%m/%d/%y') %>)<br />
		<%# end %>
	</ul>
-->

<table class="table">
	<thead>
		<tr>
			<th width="225">Price</th>
			<th width="150">Date</th>
		</tr>
	</thead>
	<tbody>

		<% @manu_price_history.each do |change| %>
		<tr>
			<td> <%= number_to_currency change.price %> </td>
			<!-- <td><%#= link_to School.find(order.school.id).name, order_path(order) %></td> -->
			<td> <%= change.start_date.strftime('%m/%d/%y') %> </td>
		</tr>
		<% end %>	
	</tbody>
</table>

</div>