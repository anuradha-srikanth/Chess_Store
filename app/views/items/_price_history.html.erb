<div id="price_history">
	<% unless @price_history.empty? %>
	<div class="panel clearfix">
		<h3>Price History</h3>

		<%= render 'list_of_prices' %>

		<%= link_to(new_item_price_path(item_id: @item.id), id: 'new_price_link', class: 'button tiny radius', remote: true) do %>
		Change Price
		<% end %>
	</div>
	<% end %>
</div>